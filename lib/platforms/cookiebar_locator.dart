import 'package:flutter/material.dart';

class CookieBar0 extends StatelessWidget {
  const CookieBar0({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container();
}
