import 'package:flutter/material.dart';

class WebWidget0 extends StatelessWidget {
  final String path;

  const WebWidget0({Key? key, required this.path}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container();
}
